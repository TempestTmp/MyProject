package myscanner;import java.io.IOException;public class Test {    public static void main(String[] args) {        final String source = "10 \n | \n 20|30";        //ThisSeparator separator = (mychar) -> mychar == '|';        String lineSeparator = System.lineSeparator();        System.err.println(lineSeparator.length());        System.err.println((int) lineSeparator.charAt(0) + " " + (int) lineSeparator.charAt(1));        System.err.println();        try {            MyScanner scan = new MyScanner("D:\\Programming\\java\\EXM\\myScanner\\src\\myscanner\\text.txt", "utf8");            //scan.updateLineSeparator("\n");            //scan.updateSeparator(separator);            while (true) {                while (scan.emptyLine()) {                    System.out.println(":EmpyteLine:");                }                if (scan.hasNext()) {                    System.out.println(scan.next());                } else {                    break;                }            }        } catch (IOException e) {            System.out.println(e.getMessage());        }    }}